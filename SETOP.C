#include<stdio.h>
#includ<stdlib.h>
#include<conio.h>
void main()
{
int set1[30],set[30],s1,s2,uni[30],inter[30],diff[30],i,ch;
clrscr();
printf("enter no of elments in 1st set:");
scanf("%d",&s1);
pintf("enter elements in 1st set(only 0s &1s):");
for(i=0;i<s1;i++)
{
scanf("%d",&set1[i]);
}
printf("\n enetr no of elme nts in 2nd set:");
scanf("%d",&s2);
printf("enete the elements in the 2nd set(only 0s and 1s):");
for(i=0;i<s2;i++)
{
scanf("%d",&set2[i]);
}




}